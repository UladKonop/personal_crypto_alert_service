<div class="row justify-content-center">
  <div class="col-md-8">
    <div class="card">
      <div class="card-header d-flex justify-content-between align-items-center">
        <h3 class="mb-0">🎯 Alert Details</h3>
        <span class="badge <%= @alert.active? ? 'bg-success' : 'bg-secondary' %> fs-6">
          <%= @alert.active? ? 'Active' : 'Inactive' %>
        </span>
      </div>
      <div class="card-body">
        <div class="row">
          <div class="col-md-6">
            <h5 class="text-primary"><%= @alert.symbol %></h5>
            <p class="text-muted mb-0">Cryptocurrency Symbol</p>
          </div>
          <div class="col-md-6 text-md-end">
            <h5 class="text-success">$<%= @alert.threshold_price %></h5>
            <p class="text-muted mb-0">Threshold Price</p>
          </div>
        </div>
        
        <hr>
        
        <div class="row">
          <div class="col-md-6">
            <div class="d-flex align-items-center mb-3">
              <span class="badge bg-<%= @alert.direction == 'above' ? 'danger' : 'info' %> me-3 fs-6">
                <%= @alert.direction == 'above' ? '⬆ ABOVE' : '⬇ BELOW' %>
              </span>
              <div>
                <strong>Direction</strong><br>
                <small class="text-muted">
                  Alert when price goes <%= @alert.direction %> $<%= @alert.threshold_price %>
                </small>
              </div>
            </div>
          </div>
          
          <div class="col-md-6">
            <div class="mb-3">
              <strong>Notification Channels</strong><br>
              <% @alert.notification_channels_array.each do |channel| %>
                <span class="badge bg-secondary me-1">
                  <%= channel == 'email' ? '📧' : '📄' %> <%= channel.titleize %>
                </span>
              <% end %>
              <% if @alert.notification_channels_array.empty? %>
                <span class="text-muted">No channels configured</span>
              <% end %>
            </div>
          </div>
        </div>
        
        <hr>
        
        <div class="row">
          <div class="col-md-6">
            <small class="text-muted">
              <strong>Created:</strong> <%= @alert.created_at.strftime("%B %d, %Y at %I:%M %p") %>
            </small>
          </div>
          <div class="col-md-6">
            <small class="text-muted">
              <strong>Last Updated:</strong> <%= @alert.updated_at.strftime("%B %d, %Y at %I:%M %p") %>
            </small>
          </div>
        </div>
      </div>
      
      <div class="card-footer d-flex gap-2">
        <%= link_to "Edit Alert", edit_alert_path(@alert), class: "btn btn-primary" %>
        <%= link_to "Back to Alerts", alerts_path, class: "btn btn-secondary" %>
        <%= button_to "Delete Alert", @alert, method: :delete, 
                      data: { confirm: "Are you sure you want to delete this alert?" }, 
                      class: "btn btn-outline-danger" %>
      </div>
    </div>
    
    <% if @alert.active? %>
      <div class="alert alert-info mt-3">
        <h6 class="alert-heading">💡 How it works</h6>
        This alert is active and will trigger when <strong><%= @alert.symbol %></strong> price goes 
        <strong><%= @alert.direction %></strong> <strong>$<%= @alert.threshold_price %></strong>. 
        When triggered, you'll receive notifications via <%= @alert.notification_channels_array.join(' and ') %>.
        <br><br>
        <small><em>The system checks prices every minute using the Binance API.</em></small>
      </div>
    <% else %>
      <div class="alert alert-warning mt-3">
        <h6 class="alert-heading">⚠️ Inactive Alert</h6>
        This alert is currently inactive and won't trigger notifications. 
        <%= link_to "Edit", edit_alert_path(@alert), class: "alert-link" %> the alert to make it active.
      </div>
    <% end %>
  </div>
</div>
