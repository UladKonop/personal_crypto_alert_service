<div class="row justify-content-center">
  <div class="col-md-8">
    <div class="card">
      <div class="card-header d-flex justify-content-between align-items-center">
        <h3 class="mb-0">
          <%= @notification_channel.channel_type == 'email' ? '📧' : '📄' %> 
          <%= @notification_channel.channel_type.titleize %> Channel
        </h3>
        <span class="badge <%= @notification_channel.active? ? 'bg-success' : 'bg-secondary' %> fs-6">
          <%= @notification_channel.active? ? 'Active' : 'Inactive' %>
        </span>
      </div>
      <div class="card-body">
        <div class="row">
          <div class="col-12">
            <h5 class="text-primary mb-3">Configuration</h5>
            
            <% if @notification_channel.channel_type == 'email' %>
              <div class="mb-3">
                <strong>Email Address:</strong><br>
                <span class="text-muted"><%= @notification_channel.config_hash['email'] %></span>
              </div>
              <div class="alert alert-info">
                <h6 class="alert-heading">📧 How Email Notifications Work</h6>
                When a price alert is triggered, an email notification will be sent to this address 
                with details about the crypto symbol, threshold price, and current price.
              </div>
            <% elsif @notification_channel.channel_type == 'log' %>
              <div class="mb-3">
                <strong>Log Level:</strong><br>
                <span class="badge bg-secondary fs-6">
                  <%= (@notification_channel.config_hash['log_level'] || 'info').titleize %>
                </span>
              </div>
              <div class="alert alert-info">
                <h6 class="alert-heading">📄 How Log Notifications Work</h6>
                When a price alert is triggered, a message will be written to the application log 
                with the specified log level. You can find these messages in the Rails logs.
              </div>
            <% end %>
          </div>
        </div>
        
        <hr>
        
        <div class="row">
          <div class="col-md-6">
            <small class="text-muted">
              <strong>Created:</strong> <%= @notification_channel.created_at.strftime("%B %d, %Y at %I:%M %p") %>
            </small>
          </div>
          <div class="col-md-6">
            <small class="text-muted">
              <strong>Last Updated:</strong> <%= @notification_channel.updated_at.strftime("%B %d, %Y at %I:%M %p") %>
            </small>
          </div>
        </div>
      </div>
      
      <div class="card-footer d-flex gap-2">
        <%= link_to "Edit Channel", edit_notification_channel_path(@notification_channel), class: "btn btn-primary" %>
        <%= link_to "Back to Channels", notification_channels_path, class: "btn btn-secondary" %>
        <%= button_to "Delete Channel", @notification_channel, method: :delete, 
                      data: { confirm: "Are you sure you want to delete this notification channel?" }, 
                      class: "btn btn-outline-danger" %>
      </div>
    </div>
    
    <% unless @notification_channel.active? %>
      <div class="alert alert-warning mt-3">
        <h6 class="alert-heading">⚠️ Inactive Channel</h6>
        This notification channel is currently inactive and won't receive any alerts. 
        <%= link_to "Edit", edit_notification_channel_path(@notification_channel), class: "alert-link" %> 
        the channel to make it active.
      </div>
    <% end %>
  </div>
</div>
